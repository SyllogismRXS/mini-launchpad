set(MINILAUNCHPAD_VERSION @PROJECT_VERSION@)

@PACKAGE_INIT@

# set_and_check(MINILAUNCHPAD_SHARE_INSTALL_DIR "@PACKAGE_SHARE_INSTALL_DIR@")
# list(APPEND CMAKE_MODULE_PATH ${MINILAUNCHPAD_SHARE_INSTALL_DIR}/cmake/MiniLaunchpad)

check_required_components(MiniLaunchpad)
